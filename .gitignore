node_modules/
dist/
*.log

# Secrets. The bran derives the issuer's signing keys — committing it hands over
# the declared identifier. The wallet keys spend real testnet funds.
.env
.env.*
!.env.example
*.bran
wallets/
artifacts/*.skey
artifacts/wallets.json
